# Типы данных
- [x] [тип `bool` как подмножество типа `int`](#тип-bool-как-подмножество-типа-int-на-самом-деле-класс-bool-наследуется-от-класса-int)
- [x] [типы данных 1](#типы-данных-1)
- [x] [типы данных 2: изменяемые и неизменяемые](#типы-данных-2-изменяемые-и-неизменяемые)
- [x] [типы данных 3: ООП](#типы-данных-3-ооп)
- [x] [ООП: полиморфизм на примере оператора `+`](#ооп-полиморфизм-на-примере)
- [ ] ООП: наследование на примере класса `bool`
- [ ] как словари используются в работе (JSON)
- [ ] генератор множеств и словарей 1:  функция `zip()`
- [ ] генератор множеств и словарей 2:  set & dict comprehension



# тип `bool` как подмножество типа `int` (на самом деле класс `bool` наследуется от класса `int`)

[Пройти викторину](https://forms.gle/ch121v4F6neQfgRT7)

![1_1_cover_bool](/images/1_1_cover_bool.jpg)

В питоне есть целые числа (0, 1, 3, 2023, -15) и булевы значения (True, False). Интересно, что булевы значения — это подмножество целых чисел. Как это узнать? Попробуйте применить знакомые операция для целых чисел, но к булевым значениям (сложение, вычитание, умножение, деление, возведение в степень). Попробуйте запустить команду print(14+True) или команду print(25*False). Если операции отработают без ошибок, то это доказывает булевы значения True и False — подможество целых чисел.

Раскрою карты. В питоне True — специальное обозначение для целого числа 1, а False — специальное обозначение для нуля.

Кстати, вы всегда можете предложить тему для поста, если напишите ваше предложение и поставите хэштег #хочутему 😉


# типы данных 1

[пройти викторину](https://forms.gle/MFJbSsa2JUmnjKDB6)

Целые числа, строки, списки, словари и другие типы данных — это важная тема в Python. Расскажу про типы данных и отвечу на вопросы:

- 🎯 Что такое тип данных?
- 🎯 Почему нельзя к словарю применить метод append? 
- 🎯 Как посмотреть все методы у конкретного типа данных и даже у вашей переменной?

![1_2_cover_data_types](/images/1_2_1_cover.png)

В Питоне есть такие типы данных: `int, bool, float, str, list, tuple, dict, set`.

Тип данных определяет множество значений, которые переменная может принимать, и множество операций, которые можно совершать с переменными данного типа.

Определение кажется сложным. Давайте рассмотрим примеры.

1️⃣ Числа (типы `int`, `float`). Множество значений целых чисел — целые числа как в математике 1, 27, -273. Множество значений вещественных чисел — вещественные числа как в математике 2.718, 3.14. Числа можно складывать, вычитать, умножать, делить (причем по-разному делить), находить остаток от деления %, возводить в степени **. Все вышеперечисленное — это операции, которые мы можем делать с числами.

2️⃣ Строки (тип `str`). Множество значений строк — любая последовательность символов в кавычках. Мы можем склеивать строки оператором +, разрезать строку и превращать ее в списков методом `.split()` и так далее. Это операции, которые мы можем делать со строками.

3️⃣ Списки (тип `list`). Мы можем добавлять новый элемент в конец списка методом `.append()`. Можем удалять элемент списка по ключу методом `.pop()`. Можем сортировать список методом `.sort()`. Это операции, которые мы можем делать со списками.

Я рассмотрел три типа данных — числа, строки, списки. Видно, что операции отличаются у разных типов данных. Поэтому нужно быть внимательным и не пытаться к целым числам применять метод `.split()` или к словарям списковый метод `append()`.

У каждого типа данных есть свой уникальный набор операций (еще это называют методами или *Attribute*). Чтобы посмотреть все методы типа данных str или все методы вашего списка `my_list`, можно использовать функцию `dir()`: `dir(str)`, `dir(my_list)`.


------------------------------викторина------------------------------

Совсем скоро будет 2 и 3 часть о типах данных ⚡️

Хотите предложить тему, пишите и добавляйте в конце хэштег #хочутему 😉

Нравится такая активность —  ставьте ❤️


# типы данных 2: изменяемые и неизменяемые 

[пройти викторину](https://forms.gle/fmWvt6UXoVVkBueS8)

![1_3_cover](/images/1_3_1_cover.png)

Всем привет! #пятничныйПитон 

В прошлом пятничном питоне рассказал о типах данных. План на сегодня:
```python
1️⃣ Напомню, какие типы данные есть, 
2️⃣ Дам определение типам данных. 
3️⃣ Объясню, что такое изменяемый и неизменяемый тип данных.
4️⃣ Покажу, как можно определить самому тип данных переменной.
```

Типы данных в Python: `int, bool, float, str, list, tuple, dict, set`.

Тип данных определяет множество значений, которые переменная может принимать, и множество операций, которые можно совершать с переменными данного типа.

В памяти компьютере переменные хранятся в ячейках памяти. Чтобы посмотреть, в какой ячейки памяти хранится ваша переменная (объект, на которых ссылается ваша переменная, если быть точнее), используют команду `id(variable)`. 

```python
numbers = list(range(5))
print(id(numbers))
```

Последняя команда напечатает число `140225181733888`.  Это индекс ячейки памяти, в котором хранится список numbers.

Изменяемый тип данных — Mutable Data Type.
Незменяемый тип данных — Immutable Data Type.

![immutable_example](/images/1_3_2_code_readable.jpg)

Неизменяемый тип данных — тип данных, значения которого нельзя изменить после создания объекта.  После каждого изменения значения неизменяемого типа данных, создается новый объект с новым значением, а старый объект удаляется из памяти. Звучит сложно. Сократим определение и свяжем новое определение в новым термином.

Неизменяемый тип данных — тип данных, для которого при изменении значения меняется его id. Неизменяемые типы данных — int, float, bool, str, tuple. Покажу, что целое число — неизменяемый тип данных 👆

В примере выше видно: id изменился. Поэтому целое число — неизменяемый тип данных.


![mutable_example](/images/1_3_3_code_readable.jpg)

Изменяемый тип данных — тип данных, для которого при изменении значений не меняется его id. Изменяемые типы данных — `list, set, dict`. Покажу, что список число — изменяемый тип данных 👆

В примере выше видно: id изменился. Поэтому список — изменяемый тип данных.

Если вы встретите такую ошибку, когда будете создавать словарь: `TypeError: unhashable type: 'list'`, то первым делом нужно проверить типы данных ваших ключей 😉


![law_to_remember](/images/1_3_4_code_readable.jpg)

Вы наверняка пробовали написать так `numbers = numbers.sort()` (спойлер: список numbers станет переменной, которая ссылается на значение `None`). Смотрите код выше. Со словарями можно тоже самое сделать.

Мы не присваиваем отсортированный список исходному списку с ожиданием, что исходный список отсортируется. Пишется `numbers.sort()`. Так делают только с переменными изменяемого типа данных. 

Вы можете возразить: как же `my_list.pop()`? Можно сохранить удаляемое по индексу значение в переменную `deleted_element = my_list.pop()`. Это одно из исключений.


### Outro

Вы узнали, как понять, что переменная изменяемого или неизменяемого типа данных. Узнали, почему нельзя отсортировать список так `numbers = numbers.sort()`. Узнали о функции `id()` для того, чтобы найти адрес, по которому хранится переменная. 

Если нравится такая активность, ставьте ❤️

В следующем пятничном Питоне расскажу про то, что такое переменная, почему в ней ничего не хранится на самом деле и причем здесь объект и ООП. Интрига. Занавес 😎


# типы данных 3: ООП 

В прошлом посте было 6 опросов на типы данных. Тема важная, поэтому сделали для вас две схемы, как не нужно писать и как нужно писать, если вы хотите изменять переменные разных типов данных:
![last_post_quiz_immutable](/images/1_3_5_outro_insight_immutable.jpg)
![last_post_quiz_mutable](/images/1_3_5_outro_insight_mutable.jpg)

Как обещал, план на сегодня:

- 🎯 что такое переменная? 
- 🎯 почему в ней ничего не хранится на самом деле?
- 🎯 что такое объект и ООП?

#пятничныйПитон

Python — Объектно-Ориентированный Язык Программирования. Почему? Весь курс мы использует переменные, а не объекты. Получается: Python должен называть Переменно-Ориентированный Язык. Так его не называют. Почему он называется именно Ориентированный на Объекты Язык. Об этом пост  😎

Термин “объект” и “класс”. Буду часто писать эти термины. Объясню сразу. что такое класс и что такое объект. 

Класс — абстрактное описание некоторой сущности. 

Объект — реализация класса. 

Мы знаем, что такое яблоко. У нас в голове есть образ яблока. Представляем его форму, размер, цвет, сорт яблока (свойства яблока называются полями). Образ яблока в голове — класс.  Яблоко, которое вы купили вчера в магазине и которое лежит у вас на столе — объект. Вы как бы оживили воображаемый образ яблока в реальном мире. В программирование такое оживление воображаемого образа называется — реализация класса.

![1_4_cover](/images/1_4_1_cover.png)

Переменная в Python не хранит ничего в себе. Переменная ссылается на объект конкретного типа данных. Пусть есть команда:

`x = 5`

Где здесь ссылка? Как понять, что переменная ссылается на объект? Использую функцию `type(x)`, чтобы определить тип (или другими словами класс) переменной `x`:

`<class 'int'>`

Видим тип данных `int` (целое число). Это мы знаем. Перед `int` написано `class`. Это намек, что Python — язык ООП.  Где здесь объект? Объект — целое число 5. Переменная `x` ссылается на объект `5` класса целых чисел.

Класс целых чисел… Тогда есть класс вещественных чисел, класс строк, класс списков, класс кортежей, класс словарей, класс множеств? Да, эти классы есть.

![show_list_class](/images/1_4_2_show_list_class.jpg)

Посмотрим на список целых чисел и узнаем, какому классу он принадлежит. Список numbers — объект класса “list”. 

У класса есть не только поля, но и методы. У каждого типа данных в Python есть методы. Некоторые из них: 

```python
👉 списковые методы .append(), .pop(), .remove(), .sort(). 
👉 строковые методы .split(), upper(), find(), center()
👉 методы словарей .get(), .items(), .keys(), .values(), .clear()
👉 методы множеств .add(), .discard()
```

Факт того, что у типов данных есть методы — еще один намек на то, что фундаментальная идея языка Python — идея ООП.

_________________________________
В пятничном питоне упоминались типы данных. В Python можно называть типы данных не только как “типы данных”, но и как “классы типов данных”. Оба варианта эквивалентны. Второй вариант намекает, какая идея лежит в фундаменте языка — идея ООП. Но чаще говорят именно “типы данных”, чтобы не перегрузить студента информацией о классах и объектах. Сначала нужно освоить Python на базовом уровне, научиться работать с циклами, коллекциями и функциями. Только потом переходить к ООП.

В Python всё является объектом. Python — Объектно-Ориентированный Язык. В новом учебном году будет Объектно-Ориентированное Программирование. Если вам интересно узнать, 

- 🎯  что такое ООП?
- 🎯  в чем преимущество такого подхода? 
- 🎯  как написать класс самому?


На продвинутом курсе вы освоите ООП. С этими знаниями вы сможете сами ответить на вопросы выше. Интересно? [Посмотрите, что еще будет в продвинутом курсе 😉](https://2035.informatics.ru/next)

# ООП: полиморфизм на примере `+`

![1_5_cover](/images/1_5_cover_polymorf.png)

```python
#что напечатает программа?

x = int(input())  #вводим 3
y = int(input())  #вводим 10
result = x + y
print(result)

#вводятся целые числа. Мы используем оператор +. Для целых чисел оператор + это оператор сложения. Поэтому x + y = 3 + 10 = 13
```

```python
#что напечатает программа, написанная выше?

x = str(input())  #вводим 3  #вводить строку можно еще так: x = input()
y = str(input())  #вводим 10
result = x + y
print(result)

#вводятся строки. Мы используем оператор +. Для строк (строковый тип данных) оператор + это оператор конкатенации. Поэтому wordOne + wordTwo = '3' + '10' = '310'
```

Уже сейчас вы знаете, что если складывать числа (переменные типа `int` или `float`), то они будут складывать как обычные числа в математике. Если складывать строки (переменные типа `str`), то строки будут "склеиваться". В двух программах выше используется один оператор — оператор +. Однако он обрабатывает по-разному переменные разного типа данных. Одним словом это называется полиморфизм!

**Полиморфизм** — возможность обрабатывать объекты (переменные) разного типа данных с помощью одного интерфейса (оператора или команды). В питоне есть такая возможность. Я показал ее выше. Поэтому в питоне есть Полиморфизм.

В чем логика называть такую возможность полиморфизмом? Поли — много, морф — форма. Полиморфизм — много форм. У оператора + как раз есть много форм. Одна форма — оператор сложения, когда он используется для обработки чисел. Другая форма — оператор конкатенации, когда он используется для обработки строк.

Теперь вы знаете одну из концепций Объектно-Ориентированного Программирования — полиморфизм!